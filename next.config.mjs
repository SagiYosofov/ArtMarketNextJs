/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [], // Make sure this includes your image domains if loading from external URLs
  },
};

export default nextConfig;
